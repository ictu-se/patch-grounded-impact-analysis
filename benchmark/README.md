# Benchmark Data

The benchmark seed repositories and task registry are not tracked in this GitHub repository.

Download the benchmark task bundle from the public artifact archive listed in `docs/DATASETS.md`, then place the downloaded files as:

```text
benchmark/
  repos/
  tasks.json
```

The tracked files in this directory are scaffold assets only: guidance templates and reusable skill files.
